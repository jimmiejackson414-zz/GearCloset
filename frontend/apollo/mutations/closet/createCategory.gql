mutation CreateCategory($name: String $unit:String $pack_id: ID) {
  createCategory(input: { name: $name unit: $unit pack_id: $pack_id}) {
    id
    name
    unit
    pack_id
    created_at
    updated_at
  }
}