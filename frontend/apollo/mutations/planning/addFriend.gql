mutation AddFriends($tripId: ID! $friends: [AddFriendsInput]!) {
  addFriends(trip_id: $tripId input: $friends) {
    id
    first_name
    last_name
    email
    avatar_url
    created_at
    updated_at
  }
}