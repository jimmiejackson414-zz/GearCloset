mutation UpdateTodo($id: ID!, $checked: Boolean, $title: String) {
  updateTodo(id: $id, input: { checked: $checked, title: $title }) {
    id
    checked
    title
    created_at
    updated_at
  }
}