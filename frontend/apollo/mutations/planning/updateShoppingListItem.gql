mutation UpdateShoppingListItem($id: ID!, $checked: Boolean, $title: String, $quantity: Int) {
  updateShoppingListItem(id: $id, input: { checked: $checked, title: $title, quantity: $quantity }) {
    id
    checked
    title
    quantity
    created_at
    updated_at
  }
}