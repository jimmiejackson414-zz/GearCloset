mutation UpdatePack($id: ID! $name: String $user_id: ID $active: Boolean $theme: String) {
  updatePack(id: $id input: { name: $name, user_id: $user_id, active: $active, theme: $theme}) {
    id
    name
    user_id
    active
    theme
    created_at
    updated_at
  }
}