mutation CreateTodo($title: String $checked: Boolean = false, $trip:ID!) {
  createTodo(input: {title:$title checked:$checked trip:{ connect: $trip}}) {
    id
    checked
    title
    created_at
    updated_at
  }
}