#import "~/apollo/fragments/todo.gql"

mutation CreateTodo($title: String $checked: Boolean = false, $trip:ID!) {
  createTodo(input: {title:$title checked:$checked trip_id: $trip}) {
    ...todo
  }
}